# API URL for development
# For local development on both Windows and macOS, use localhost.
# If you need to test with external devices (e.g. mobile), create a .env.local file with your specific IP.
VITE_API_URL=http://localhost:2530
